import React, { Component } from 'react';
import PropTypes from 'prop-types';

class About extends Component {
    state = {  };

    render() {
        return (
           <div>We are Females</div> 
        );
    }
}

export default About ;